<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App</title>
    <link rel="stylesheet" href="//stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="//stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #e0eafc 0%, #cfdef3 100%);
        }

        .todo-header {
            background: #007bff;
            color: #fff;
            border-radius: 1rem 1rem 0 0;
            padding: 2rem 1rem 1rem 1rem;
            text-align: center;
        }

        .todo-form input {
            border-radius: 2rem;
        }

        .todo-card {
            border-radius: 1rem;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
            margin-bottom: 1rem;
        }

        .empty-illustration {
            width: 120px;
            margin: 2rem auto 1rem auto;
            display: block;
        }
    </style>
</head>

<body>
    <div class="container my-5 p-0 mx-auto bg-white shadow" style="max-width:600px;">
        <div class="todo-header">
            <h1><i class="fa fa-check-square-o"></i> Todo App</h1>
            <form method="POST" action="/" autocomplete="off" class="todo-form mt-4">
                <div class="form-row align-items-center">
                    <div class="col-8">
                        <input class="form-control form-control-lg" type="text" name="task" placeholder="新しいタスクを追加">
                    </div>
                    <div class="col-4">
                        <button type="submit" class="btn btn-light btn-lg w-100"><i class="fa fa-plus"></i> 追加</button>
                    </div>
                </div>
            </form>
        </div>
        <div class="p-4">
            <% if(Object.keys(tasks).length > 0) { %>
            <% tasks.forEach(todo => { %>
            <div class="card todo-card">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="card-title mb-1"><%= todo.task %></h5>
                        <p class="card-text text-muted mb-0">
                            <small>
                                作成: <%= moment(todo.created_at).format('YYYY/MM/DD HH:mm') %>
                            </small>
                        </p>
                    </div>
                    <a href="javascript:;" onclick="this.children[0].submit()" class="text-danger ml-3" title="削除">
                        <form method="POST" action="/todo/destroy">
                            <input type="hidden" name="_key" value="<%= todo._id %>" />
                        </form>
                        <i class="fa fa-trash-o fa-lg"></i>
                    </a>
                </div>
            </div>
            <% }) %>
            <% } else { %>
            <img src="https://cdn-icons-png.flaticon.com/512/4076/4076549.png" class="empty-illustration" alt="Empty">
            <div class="text-center text-secondary">
                <strong>タスクがありません。<br>上のフォームから追加してください。</strong>
            </div>
            <% } %>
        </div>
    </div>
</body>

</html>